import { Physics } from '@react-three/rapier'
import useGame from './stores/useGame.jsx'
import Lights from "@/components/3d/Lights.tsx"
import { Level } from "@/components/3d/Level.tsx"
import Player from './Player.jsx'
import Orbit from './Orbit.jsx'
import { Canvas } from "@react-three/fiber";
import { Leva } from "leva";
import Experience from "./Experience";
import { useRef, useState } from "react";
import { FaLocationArrow } from "react-icons/fa6";
import { KeyboardControls, OrbitControls } from "@react-three/drei";
import Interface from "./Interface";
import { GizmoViewport } from "@react-three/drei";
import { GizmoHelper } from "@react-three/drei";
import { Grid } from "@react-three/drei";
import Bubbles from "./Bubbles";
import Link from "next/link";
import Popups from "./Bubbles";
import { FloatingDockDemo } from "./floatingdockDemo"
import AccordionUsage from './accordion'
import ModeToggleButton from "./modeToggle";
import useCameraStore from './stores/usecamerastore';
import useModeStore from './stores/usemodestore';
import { PerspectiveCamera } from "@react-three/drei";
import { useModalStore } from './stores/useModalStore.jsx'
import { View } from '@react-three/drei'
import create from 'zustand'
import { OrthographicCamera } from '@react-three/drei'
import { forwardRef } from 'react'
import { Level2 } from '@/components/3d/Level2.tsx'


 const Scene = () => {
          return <>

             <Physics debug={false}>
                      <Lights />
                      <Level />
                      <Player />
                  </Physics>
      <PerspectiveCamera
        makeDefault
        fov={45}
        near={0.1}
        far={200}
        position={[0, 50, 100]}
      />


      </>
}

 const Scene2 = () => {
          return <>
             <Physics debug={false}>
                      <Lights />
                      <Level2 />
              </Physics>                 

              <OrbitControls 
                enablePan={true}       // Allow panning (default: true)
                enableZoom={true}      // Allow zooming (default: true)
                minDistance={5}        // Minimum zoom distance
                maxDistance={50}       // Maximum zoom distance
                maxPolarAngle={Math.PI / 2}  // Limit vertical orbit (in radians)
              />

               <PerspectiveCamera
                  makeDefault
                  fov={45}
                  near={0.1}
                  far={200}
                  position={[0, 50, 30]}
                />
      </>
}


export default function Experience() {

    const {isModalOpen} = useModalStore();

    const container = useRef();

    
    return <>
    <main ref={container}  
      style={{ width: '100%', height: '100vh', overflow: 'hidden' }}
>

        {/* <color args={['#FEFCF6']} attach="background" /> */}


        <View style={{ position: 'absolute', width: 700, height: 700 , top:0 , left:0 }}>
          <Scene />
        </View>

        <View style={{ position: 'absolute', width: 700, height: 700 , top:0 , right:0 }}>
          <Scene2 />
        </View>


        <Canvas
  shadows
  style={{ width: '100%', height: '100%', background: 'linear-gradient(180deg, #245792, #0A315F)' }}
  eventSource={container}
>

              <Leva hidden />

               <View.Port />


              {/* <Popups /> */}
        </Canvas>



        
</main>
    </>
}